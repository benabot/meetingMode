# TODO

- Wire real permission checks in `PermissionService`.
- Persist presets and the selected preset locally.
- Replace launch and restore stubs with permission-aware macOS implementations.
- Add a lightweight overlay window implementation.
- Decide whether onboarding is needed before wiring sensitive automation.
